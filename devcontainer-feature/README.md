# Pi Dev Container Feature

Installs Pi with the portable profile from this repository. Container-local mutable state persists under the workspace's `.pi` directory. The Feature has no host bind mounts.

```json
"features": {
  "ghcr.io/jjlinares/pi/pi:1": {}
}
```

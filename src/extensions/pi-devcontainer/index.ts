import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Extension scaffold. Routing implementation intentionally pending. */
export default function piDevcontainer(_pi: ExtensionAPI): void {}
